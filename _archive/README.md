# Arquivo histórico

Materiais preservados fora da raiz do projeto:

- `root-sql/`: SQLs manuais e backups legados.
- `root-data/`: saídas de diagnóstico geradas.
- `config-snapshots/`: cópias antigas de configuração.

Não use esses arquivos como fonte de verdade sem validar o estado atual do ambiente.
